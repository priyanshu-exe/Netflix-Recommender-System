{
  "buildCommand": "apt-get update && apt-get install -y libxml2-dev libxslt-dev && pip install -r requirements.txt",
  "functions": {
    "api/main.py": {
      "memory": 3008,
      "maxDuration": 60
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "api/main.py"
    }
  ]
}
